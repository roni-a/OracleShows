SELECT TRUNC(TIME) AS AT,
       SUBSTR(PROCESS, 1, 14) PROCESS,
       MESSAGE,
       COUNT(*) AS NumOfTimes
FROM   BILL.E_GENERAL_ERROR
WHERE  TIME > SYSDATE - 3
GROUP  BY TRUNC(TIME), SUBSTR(PROCESS, 1, 14), MESSAGE
ORDER  BY TRUNC(TIME)  DESC, SUBSTR(PROCESS, 1, 14)
/
